name: Build & Push Dashboard Image

on:
  push:
    branches:
      - master
    tags-ignore:
      - '*.*'

env:
  IMAGE_REGISTRY: quay.io
  IMAGE_NAME: codeready-toolchain/tarsy-dashboard
  REGISTRY_USER: "codeready-toolchain+tarsy_push"
  REGISTRY_PASSWORD: ${{ secrets.QUAY_PASSWORD }}

jobs:
  build-and-push-dashboard:
    name: Build and Push Dashboard Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set short SHA
        id: short-sha
        run: echo "COMMIT_SHORT_SHA=$(git rev-parse --short ${GITHUB_SHA})" >> ${GITHUB_ENV}

      - name: Build dashboard image
        id: build-image
        uses: redhat-actions/buildah-build@v2
        with:
          image: ${{ env.IMAGE_NAME }}
          tags: latest ${{ env.COMMIT_SHORT_SHA }}
          context: ./dashboard
          containerfiles: |
            ./dashboard/Dockerfile
          build-args: |
            VITE_API_BASE_URL=
            VITE_WS_BASE_URL=
            VERSION=${{ env.COMMIT_SHORT_SHA }}

      - name: Log into quay.io
        uses: redhat-actions/podman-login@v1
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          username: ${{ env.REGISTRY_USER }}
          password: ${{ env.REGISTRY_PASSWORD }}

      - name: Push to quay.io
        id: push-to-quay
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build-image.outputs.image }}
          tags: ${{ steps.build-image.outputs.tags }}
          registry: ${{ env.IMAGE_REGISTRY }}

      - name: Print image URL
        run: |
          echo "✅ Dashboard image pushed successfully!"
          echo "Image: ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}"
          echo "Tags: latest, ${{ env.COMMIT_SHORT_SHA }}"
          echo "Full URLs:"
          echo "  - ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          echo "  - ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.COMMIT_SHORT_SHA }}"

