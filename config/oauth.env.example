# TARSy OAuth2 Environment Variables
#
# This file provides OAuth2 configuration for container deployments.
# Copy and customize for your deployment:
#   cp config/oauth.env.example config/oauth.env
#   vim config/oauth.env
#   make containers-deploy  # Automatically loaded!
#
# IMPORTANT: 
# - config/oauth.env is in .gitignore - your secrets stay local!
# - The Makefile automatically loads this file if it exists
# - No need to manually source it!

# Required: GitHub OAuth App Credentials
# Create your OAuth App at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:8080/oauth2/callback
export OAUTH2_CLIENT_ID="your-github-oauth-client-id"
export OAUTH2_CLIENT_SECRET="your-github-oauth-client-secret"

# Optional: GitHub Access Control
# Restrict access to users in specific GitHub org/team
export GITHUB_ORG="codeready-toolchain-teams"
export GITHUB_TEAM="observability"

# Optional: Override deployment settings
# export ROUTE_HOST="localhost:8080"  # Change for custom hostname
# export COOKIE_SECURE="false"        # Set to "true" for HTTPS

