# Development Environment - Simplified
# Direct backend connection via Vite proxy (no containers, no auth)

# API/WebSocket URLs - Leave empty to use relative URLs + Vite proxy
VITE_API_BASE_URL=
VITE_WS_BASE_URL=

# Frontend Development Server
VITE_DEV_SERVER_HOST=localhost
VITE_DEV_SERVER_PORT=5173

# Environment
VITE_NODE_ENV=development
